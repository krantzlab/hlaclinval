% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{min_detectable_or}
\alias{min_detectable_or}
\title{Minimum detectable odds ratio at a target power}
\usage{
min_detectable_or(
  n_case,
  carriage,
  alpha = 0.05,
  ratio = 1,
  n_ctrl = NULL,
  power = 0.8,
  or_max = 80
)
}
\arguments{
\item{n_case}{Number of cases.}

\item{carriage}{Control / population carriage frequency (exposure prevalence),
in (0, 1).}

\item{alpha}{Significance threshold. For a Bonferroni correction over \code{m}
tests, pass \code{0.05 / m}.}

\item{ratio}{Controls per case (ignored if \code{n_ctrl} is supplied).}

\item{n_ctrl}{Number of controls. Overrides \code{ratio} when supplied.}

\item{power}{Target power (default 0.80).}

\item{or_max}{Upper search bound for the odds ratio (default 80).}
}
\value{
The minimum detectable odds ratio, or \code{NA} if \code{power} is not
reachable below \code{or_max}.
}
\description{
Finds the smallest odds ratio detectable at \code{power} using exact Fisher power
(\code{\link[=fisher_power]{fisher_power()}}). Sample size is treated as fixed (as in retrospective
registry/biobank studies); this is the recommended quantity for
STROBE/STREGA item 10 rather than post-hoc observed power.
}
\examples{
# Minimum detectable OR: 29 cases, 2:1, 4.1\% carriage, alpha 0.05
min_detectable_or(n_case = 29, carriage = 0.041, ratio = 2)

# With Bonferroni over 100 alleles
min_detectable_or(n_case = 29, carriage = 0.041, ratio = 2, alpha = 0.05 / 100)
}
