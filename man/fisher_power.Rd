% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{fisher_power}
\alias{fisher_power}
\title{Exact power for a case-control carrier-status association}
\usage{
fisher_power(
  n_case,
  carriage,
  or,
  alpha = 0.05,
  ratio = 1,
  n_ctrl = NULL,
  reject = NULL
)
}
\arguments{
\item{n_case}{Number of cases.}

\item{carriage}{Control / population carriage frequency (exposure prevalence),
in (0, 1).}

\item{or}{Odds ratio to detect.}

\item{alpha}{Significance threshold. For a Bonferroni correction over \code{m}
tests, pass \code{0.05 / m}.}

\item{ratio}{Controls per case (ignored if \code{n_ctrl} is supplied).}

\item{n_ctrl}{Number of controls. Overrides \code{ratio} when supplied.}

\item{reject}{Optional precomputed rejection matrix from
\code{\link[=build_reject]{build_reject()}}; supply it to avoid recomputation across a grid of ORs.}
}
\value{
Power (numeric scalar in [0, 1]).
}
\description{
Computes the power of the two-sided Fisher exact test to detect a
carrier-status association, by enumerating the exact rejection region over
all possible 2x2 tables and summing the joint probability under two
independent binomials (carriers among cases and among controls). This is
exact (no simulation, no normal approximation) and conservative at small
sample sizes.
}
\examples{
# 29 cases, 2:1 controls, 4.1\% carriage, single test
fisher_power(n_case = 29, carriage = 0.041, or = 16.4, ratio = 2)
}
