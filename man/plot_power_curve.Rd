% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputs.R
\name{plot_power_curve}
\alias{plot_power_curve}
\title{Plot the exact power curve versus odds ratio}
\usage{
plot_power_curve(
  n_case,
  carriage,
  alpha = 0.05,
  ratio = 1,
  n_ctrl = NULL,
  target = 0.8,
  or_hi = 40,
  mark_or = NULL,
  col = "#3F6699"
)
}
\arguments{
\item{n_case}{Number of cases.}

\item{carriage}{Control / population carriage frequency (exposure prevalence),
in (0, 1).}

\item{alpha}{Significance threshold. For a Bonferroni correction over \code{m}
tests, pass \code{0.05 / m}.}

\item{ratio}{Controls per case (ignored if \code{n_ctrl} is supplied).}

\item{n_ctrl}{Number of controls. Overrides \code{ratio} when supplied.}

\item{target}{Target power line to draw (default 0.80).}

\item{or_hi}{Upper OR for the x-axis (default 40).}

\item{mark_or}{Optional observed OR to mark.}

\item{col}{Line/marker colour.}
}
\value{
Invisibly the minimum detectable OR at \code{target}.
}
\description{
Power as a function of the odds ratio at a fixed sample size, with the target
power line and the minimum detectable OR marked. The figure for the
STROBE/STREGA minimum-detectable-effect statement. Pure base-R graphics.
}
\examples{
plot_power_curve(n_case = 29, carriage = 0.041, ratio = 2)
}
