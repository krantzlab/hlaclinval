% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictive_ci.R
\name{predictive_ci}
\alias{predictive_ci}
\title{Predictive values with confidence intervals from a 2x2 table}
\usage{
predictive_ci(
  cases_carrier,
  cases_noncarrier,
  controls_carrier,
  controls_noncarrier,
  conf.level = 0.95,
  prevalence = NULL
)
}
\arguments{
\item{cases_carrier, cases_noncarrier}{Carrier and non-carrier counts in cases.}

\item{controls_carrier, controls_noncarrier}{Carrier and non-carrier counts in
controls.}

\item{conf.level}{Confidence level (default 0.95).}

\item{prevalence}{Optional incidence of the reaction among exposed; if given,
prevalence-adjusted PPV/NPV are added.}
}
\value{
A list with \code{epir} (the full \code{\link[epiR:epi.tests]{epiR::epi.tests()}} object, including
sensitivity, specificity, PPV, NPV and their CIs) and, when \code{prevalence} is
supplied, \code{ppv_adj} and \code{npv_adj}.
}
\description{
Computes sensitivity, specificity, PPV, NPV and number needed to test with
confidence intervals from raw case-control carrier counts, by delegating to
\code{\link[epiR:epi.tests]{epiR::epi.tests()}}. Confidence intervals require cell counts (not summary
rates), so this is the count-based companion to \code{\link[=ppv_npv_nnt]{ppv_npv_nnt()}}, which takes
population rates and returns point estimates.
}
\details{
The 2x2 is laid out as test (carrier status) by outcome (case/control):
\tabular{lcc}{
\tab case      \tab control     \cr
carrier   \tab \code{cases_carrier}   \tab \code{controls_carrier}   \cr
non-carrier \tab \code{cases_noncarrier} \tab \code{controls_noncarrier} \cr
}

Note: in a case-control sample, PPV/NPV from \code{epi.tests} reflect the sampled
case:control ratio, not the population. To obtain population PPV/NPV at a
realistic incidence, supply \code{prevalence} (incidence among exposed); the
function then also returns prevalence-adjusted PPV/NPV using the
sensitivity/specificity point estimates.
}
\examples{
\dontrun{
  # Konvinse vancomycin DRESS: 19/23 cases, 0/46 controls carried A*32:01
  predictive_ci(cases_carrier = 19, cases_noncarrier = 4,
                controls_carrier = 0, controls_noncarrier = 46)
}
}
