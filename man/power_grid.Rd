% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputs.R
\name{power_grid}
\alias{power_grid}
\title{Minimum detectable OR across a grid of designs}
\usage{
power_grid(n_case, carriage, ratio = 1, alpha = 0.05, power = 0.8, ref_or = 10)
}
\arguments{
\item{n_case}{Vector of case counts.}

\item{carriage}{Vector of control/population carriage frequencies.}

\item{ratio}{Vector of controls-per-case.}

\item{alpha}{Vector of significance thresholds.}

\item{power}{Target power for the detectable OR (default 0.80).}

\item{ref_or}{Reference OR at which to also report power (default 10).}
}
\value{
A data frame with one row per scenario: \code{n_case}, \code{ratio}, \code{n_ctrl},
\code{carriage}, \code{alpha}, \code{mdor}, and \code{power_at_ref_or}.
}
\description{
Tabulates the minimum detectable odds ratio (and power at a reference OR)
over combinations of case count, control:case ratio, carriage, and alpha.
Returns a tidy data frame for a power-justification table or supplement.
}
\examples{
power_grid(n_case = c(20, 29, 50), carriage = 0.04, ratio = 2, alpha = 0.05)
}
