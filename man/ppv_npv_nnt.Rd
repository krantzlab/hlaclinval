% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictive.R
\name{ppv_npv_nnt}
\alias{ppv_npv_nnt}
\title{Predictive values, number needed to test, and carrier risk}
\usage{
ppv_npv_nnt(sensitivity, carriage, incidence, per_flagged = 1000)
}
\arguments{
\item{sensitivity}{Proportion of cases carrying the allele (test sensitivity),
in [0, 1].}

\item{carriage}{Population carriage frequency of the allele among the exposed
population, in (0, 1).}

\item{incidence}{Incidence of the reaction among exposed individuals, as a
proportion (e.g. \code{1/1000}). See \code{\link[=one_in]{one_in()}}.}

\item{per_flagged}{Denominator for the natural-frequency breakdown
(default 1000 flagged carriers).}
}
\value{
An object of class \code{hla_translation}: a list with \code{ppv}, \code{npv},
\code{nnt}, \code{carrier_risk}, \code{ppv_ceiling}, \code{enrichment_vs_pop}, \code{miss_fraction},
\code{noncarrier_risk}, \code{react_per_flagged}, \code{unnecessary_per_flagged}, and
\code{inputs}. A \code{print()} method gives a readable summary.
}
\description{
Translates an HLA risk-allele association into clinical-utility metrics for
use as a screening test. The positive predictive value equals the absolute
risk of the reaction in an exposed carrier.
}
\details{
Identities used (carrier/dominant coding; rare reaction, so OR approx RR):
\deqn{PPV = sensitivity \times incidence / carriage}
\deqn{NPV = 1 - (1 - sensitivity) \times incidence / (1 - carriage)}
\deqn{NNT = 1 / (sensitivity \times incidence)}
The PPV ceiling (at 100\\% sensitivity) is \code{incidence / carriage}, and the
carrier risk is enriched over the population incidence by the factor
\code{sensitivity / carriage}.
}
\examples{
# Lamotrigine, HLA-A*32:01
ppv_npv_nnt(sensitivity = 0.414, carriage = 0.06, incidence = one_in(1000))
}
